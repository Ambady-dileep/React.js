// // every() ➡ Checks if all elements meet the condition.

// const nums = [2, 4, 6];
// console.log(nums.every(n => n % 2 === 0)); // true

// every() – (Returns true if all elements match the condition)
// // Check if all numbers are greater than 0 in [1, 2, 3, 4].
// const arr = [1, 2, 3, 4]
// const result = arr.every(x=>x>0)
// console.log(result)

// // Check if all numbers are even in [2, 4, 6, 8].
// const arr1 = [2, 4, 6, 8];
// const result1 = arr1.every(num => num % 2 === 0);
// console.log(result1); // true

// // Check if all words have length > 2 in ["hi", "hello", "hey"].
// const arr2 = ["hi", "hello", "hey"];
// const result2 = arr2.every(word => word.length > 2);
// console.log(result2); // false (because "hi" has length 2)

// // Check if all numbers are less than 100 in [10, 20, 30, 40].
// const arr3 = [10, 20, 30, 40];
// const result3 = arr3.every(num => num < 100);
// console.log(result3); // true

// // Check if all names start with "A" in ["Ambady", "Anu", "Amal"].
// const arr4 = ["Ambady", "Anu", "Amal"];
// const result4 = arr4.every(name => name.startsWith("A"));
// console.log(result4); // true
